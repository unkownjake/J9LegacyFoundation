export interface HomePageCardContent {
  id: string;
  title: string;
  description: string;
  icon: string;
  link: string;
  image: string;
  verticalPosition: string;
}

export interface HeroContent {
  title: string;
  description: string;
}
